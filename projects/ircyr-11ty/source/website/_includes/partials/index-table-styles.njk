<style>
.index-notes {
    background: linear-gradient(to right, #fffbeb, #fef3c7);
    border-left: 4px solid #f59e0b;
    padding: 1rem 1.5rem;
    margin-bottom: 1.5rem;
    border-radius: 4px;
}

.index-notes p {
    margin: 0;
    color: #92400e;
    font-size: 0.95rem;
}

.entry-count-info {
    color: #666;
    font-size: 0.9rem;
    margin-bottom: 1rem;
}

.index-table {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    margin-top: 1rem;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: var(--shadow-sm);
}

.index-table thead {
    background: linear-gradient(to bottom, #2c5282, #2d3748);
    color: white;
}

.index-table th {
    padding: 1rem;
    text-align: left;
    font-weight: 600;
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border: none;
    color: white;
}

.index-table td {
    padding: 0.75rem 1rem;
    border-bottom: 1px solid var(--border-color);
    vertical-align: top;
}

.index-table tbody tr {
    background: white;
    transition: all 0.2s ease;
}

.index-table tbody tr:hover {
    background: var(--bg-light);
}

.index-table tbody tr:last-child td {
    border-bottom: none;
}

.index-table td:first-child {
    font-weight: 500;
    color: var(--primary-color);
}

.index-table .references {
    font-size: 0.9rem;
    line-height: 1.6;
}

.index-table .references a {
    color: var(--accent-color);
    text-decoration: none;
}

.index-table .references a:hover {
    text-decoration: underline;
}

.index-table .line-ref {
    color: #888;
    font-size: 0.85rem;
}

.index-table .abbr-col {
    font-family: 'Times New Roman', Times, serif;
    font-size: 1.1rem;
}

.index-table .expansion-col {
    font-style: italic;
    color: #555;
}

@media (max-width: 768px) {
    .index-table {
        font-size: 0.875rem;
    }

    .index-table th,
    .index-table td {
        padding: 0.5rem;
    }
}
</style>
