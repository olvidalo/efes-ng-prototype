{#
  Example index page template for a "persons" index.

  To use:
  1. Rename this file to persons.njk
  2. Uncomment the persons index definition and extraction template in indices-config.xsl
  3. Uncomment the aggregate-indices node in pipeline.xml
  4. Update documentBasePath below to match where your document pages live
     (e.g. "/en/seals" or "/en/inscriptions")
#}
---
layout: layouts/base.njk
title: Persons
---

{% set documentBasePath = "/en/inscriptions" %}
{% set indexData = indices.persons %}
{% include "partials/index-table.njk" %}
